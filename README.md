
  # Game Testing System
  This is prototype for Game Testing System.

  ## Running the code
  Run `npm i` or *`instal.bat` file* to install the dependencies.
  Run `npm run dev` or *`start.bat` file* to start the development server.